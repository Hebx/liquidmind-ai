# Legacy `cre-workflow/` Directory

This directory is deprecated and retained only as historical reference material.

## Do Not Use This Path For Current Work

- The canonical CRE package for the current milestone is `liquidmind/agentic-liquidity`.
- Do not use `cre-workflow/` as the implementation target for new work, validation, or milestone evidence.
- If you need to validate the active CRE workflow package, run:

```bash
cd ../liquidmind/agentic-liquidity
npm install
npm run validate:real
```

## Legacy Local Demo Notes

- Any mock simulation flow retained under the old or new package paths is local-demo material only.
- Mock output is not evidence of the live milestone path.
- A2A, x402, and broader cross-chain execution ideas in legacy materials should be treated as deferred or historical unless separately verified elsewhere in the repo.

## Why This Still Exists

- historical context
- old experiments that have not been deleted yet
- reference material during the migration to the canonical `liquidmind/agentic-liquidity` path
