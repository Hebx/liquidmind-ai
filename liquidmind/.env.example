# CRE project-level environment for `cre workflow ...` commands.
# Copy to `liquidmind/.env` before running simulation or deploy commands.

# Base Sepolia RPC used by the active LiquidMind CRE workflow.
BASE_SEPOLIA_RPC=https://base-sepolia.g.alchemy.com/v2/YOUR_KEY
